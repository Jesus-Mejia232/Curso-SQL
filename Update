# Con update actualizamos datos, como su nombre lo dice:

update turnosmedicos(nombreColumna ) 
set horario(NombreCampo) = NuevoDato, motivo = "dolor de panza"
where id_turno = 12; # El cambio va a ser realizado a los registros con el id "12"



# Ahora mostramos la tabla ya actualizada, en pantalla:

select * from turnosmedicos