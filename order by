# De esta forma estamos ordenando los datos de manera descendente, 
select * from products
order by productsName desc nulls last 


# Con la siguiente consulta estamos diciendo que, selcecione los productos, y los repetidos, que los elimine,
al usar la clausula "distinct" en la campo product name  
select DISTINCT ProductName as produc FROM Products
order by ProductName asc nulls last


select * from  ProductName
order by random() # Se ponen los parentesis ya que es una función
una vez está esta función, no podemos agregar más parámetros, tales como el "desc nulls first" o el "asc nulls last"
quizás permita, pero no servirá de nada poner esos parámetros

